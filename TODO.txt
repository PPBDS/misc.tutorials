## Harshil and Var


### Var

`marginaleffects`: https://marginaleffects.com/

Read chapters 2 and 3 to get background.
Avoid chapters 4 and 7
Key chapters are 5 and 6 (Don;t worry about tests) Don't worry about slopes.
Check out chapter 15

Difference between marginal and conditional predictions is critical! And confusing!
Difference between `by` and `condition` is critical.

Maybe basing the whole thing on 15.1 and 15.2 makes sense.

And counter-factual stuff is interesting too!

### Harshil

`tidymodels`: https://www.tmwr.org/

Maybe this is better source? https://www.tidymodels.org/start/

Link to both.

Goal is to understand the overall tidymodels framework. How does it work? What does it mean?

useful to review chapters 1 through 3

Students need to understand linear_reg(), logistic, multinomial and ordered. 

Then discuss set_engine()

fit() and what that means. And then we have the model object. 

parsnip package is important. Discuss mode. See https://parsnip.tidymodels.org/

Maybe discuss tidy() for looking at coefficients.




Our next project is to write a few more misc tutorials. We need to give students more practice in just making things. Advice:

    * Start by reading: https://github.com/PPBDS/ai-rules/blob/main/claude-md/tutorials/CLAUDE.md. As you can see, you can just tell your favorite AI to follow these instructions and it will do a pretty good first draft!

    * You can assume that students have done all the r4ds tutorials, so they have a good amount of practice.

    * The big change here is that we should have the final project be a website instead of a single GitHub page. That is, you can assume that students have the vscode tutorials related to websites, or at least the first one.

    * Therefore, you need to give a bit more thought to what the website will look like. (This is different from the r4ds tutorials, in which we just slap together a couple of graphics in the one published page.)

    * Have the students add some text to the website, with the help of AI, of course.

    * The tutorial names will be the basic topic, like `baseball`.


### Progress

    * baseball — DONE (first project-tier tutorial; its project type is a
      multi-page Quarto website). Site is index + home-runs + sluggers, using
      the Lahman R data package directly (no extdata/manifest). Future
      project-tier tutorials may instead build Shiny apps, Quarto dashboards,
      etc. — same tier, different project type. Anomaly: total HR appears to collapse in 1981/1994/
      2020, but those are shortened seasons (strike/COVID) — normalizing to HR per
      game reveals the real eras (dead-ball, 1920 jump, WWII dip, steroid era,
      2019 juiced ball). Sluggers page ties 9 of the top-15 career HR leaders to
      the steroid-era spike. See CLAUDE.md "Website tutorials" for the recorded
      overrides (website artifact, AI-prose-as-content, topic-driven not storage-
      driven, package-data exemption from extdata).
      Open follow-ups for baseball:
        - Lahman updates ~yearly; the "1.39 in 2019 / 762 Bonds" numbers in the
          knowledge drops are version-pinned (Lahman 14.0.0, data through 2025).
          Re-check the KD figures if the package is bumped.
        - Consider a Statcast/baseballr appendix to give caching a genuinely
          expensive chunk (currently the cache arc is on a fast aggregation,
          justified as habit-building like .gitignore).

### Possible tutorial topics

I am flexible on the topics you might select. Anything which at least some college students might be interested in is good. Especially interested in topics which students might use for final projects.

    * baseball (or any other sport) — DONE, see Progress above.

    * text (like tidytext package, using babynames, perhaps)

    * stocks

    * Bitcoin

    * weather

    * US maps (will probably follow `census`, so you can assume that a student has done that tutorial).

### Other considerations

    * A tutorial about working with large, complex projects? And fixing it? Each topic would involve forking an already existing project and then making a bunch of changes, perhaps including getting the raw data.

    * Cover material from "What They Forgot to Teach You About R": https://rstats.wtf/ Might cover things like the **here** package. This is, sort of, a follow on to the vscode infrastructure package.



## Alec

Fix census. There should be a data/ directory which includes the rds files which we use. Don't forget to save, in the setup chunk, the commented out code which creates those data sets. -- AR: DONE.

https://ppbds.github.io/tutorial.helpers/articles/ai.html#data

No data sets, or even a census directory in extdata. -- AR: DONE.

Clean up census tutorial. Lots of questions telling AI to download some data, building question to question. -- AR: DONE.

Data on education by state -- AR: DONE.



## Items

Data sets should go in inst/tutorials/<name>/data. Then our code would look like student code. And we could get rid of inst/extdata (except for the documentation). But, if we go this path, we can't put the repo on CRAN. But who really cares?

Find better data sets than the ones we are using. We want something in which there is a weird mystery (plots which we can't understand) which we eventually solve and, in the end, create a plot which teaches us something.

`census` should have a data directory and various rds files should be placed there, with the code set to cache so that it doesn't run every time. Later QMD code then just loads the rds file it wants.

Tutorials now are about concepts, not books.

 Would options(pillar.print_max = Inf, pillar.print_min = Inf) make the use of df_print: default not necessary? Which is best? Again, goal is for printed out tibbles to look the same in the tutorial html as they look in the Console.

Should we be using duckdplyr more and/or in place of other databases?

